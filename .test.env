# Test Configuration for E2E Tests
# Copy this to .test.env and fill in values

# Test user credentials (auto-created in database)
TEST_USER_EMAIL=adamaubry.pro@gmail.com          # Used as username (schema uses 'username' field)
TEST_USER_PASSWORD=adamaubryprogmailom9A@    # Plain text - will be bcrypt hashed
TEST_USER_NAME=adamaubry.pro     # Workspace name
TEST_WORKSPACE_SLUG=test     # Workspace URL slug

# Git repository configuration
TEST_REPO_PATH=/home/adam/markdown_projects/draehi/test-data/logseq-docs-graph
TEST_REPO_BRANCH=master

# Database connection (defaults to .env DATABASE_URL if not set)

# Application URL for testing
TEST_APP_URL=http://localhost:3000
AUTOMATED_SYNC_WAIT=true
