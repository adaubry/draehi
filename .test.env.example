# Test Configuration for E2E Tests
# Copy this to .test.env and fill in values

# Test user credentials (auto-created in database)
TEST_USER_EMAIL=testuser          # Used as username (schema uses 'username' field)
TEST_USER_PASSWORD=testpass123    # Plain text - will be bcrypt hashed
TEST_USER_NAME=Test Workspace     # Workspace name
TEST_WORKSPACE_SLUG=testuser      # Workspace URL slug

# Git repository configuration
TEST_REPO_PATH=/home/user/draehi/test-data/logseq-docs-graph
TEST_REPO_BRANCH=master

# Database connection (defaults to .env DATABASE_URL if not set)
# TEST_DATABASE_URL=postgresql://user:password@localhost:5432/draehi_test

# Application URL for testing
TEST_APP_URL=http://localhost:3000
